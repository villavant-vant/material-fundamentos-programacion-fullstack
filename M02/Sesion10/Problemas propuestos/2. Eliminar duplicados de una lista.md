# Problema propuesto 2: Eliminar números duplicados

Dado un `ArrayList<Integer>` con números repetidos (por ejemplo `{3, 5, 3, 8, 5, 9, 1, 8}`), crea un método que retorne un nuevo `ArrayList<Integer>` sin elementos duplicados, conservando el orden original de aparición.

Imprime la lista original y la lista sin duplicados.
